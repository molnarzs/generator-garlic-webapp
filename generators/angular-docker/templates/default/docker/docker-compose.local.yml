version: '2'

services:
  <%= c.webpackServerName %>:
    build: ..
    volumes:
      - "../src:/app/src"
      - "../dev-site:/app/dev-site"
      - "../hooks:/app/hooks"
    environment:
      - NODE_ENV