version: '2'

services:
  <%= conf.webpackServerName %>:
    build:
      context: ..
      dockerfile: Dockerfile.dev
    volumes:
      - "../src:/app/project/src"
      - "../hooks:/app/project/hooks"
      - "../reports:/app/reports"
      - "../dist:/app/dist"
    environment:
      - NODE_ENV
      - CI
    command: "npm start"
