version: '2'

services:
  <%= c.scope %>.<%= c.appNameKC %>.dev:
    build:
      context: ../
      dockerfile: Dockerfile
    env_file:
      - "./service.env"
    volumes:
      - "../:/app/project"
    command: "gulp"
